import styled from "styled-components";

export const NotificationsStyled = styled.div`
  ul.activeNotifications {
    padding: 0;
    margin: 0 0 2em;

    li {
      line-height: 1.35;
      text-align: center;
      padding: 0;
      margin: 0 0 1em;
      list-style-type: none;
    }
  }
`;
